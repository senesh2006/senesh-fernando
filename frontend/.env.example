# Next.js portfolio API (senesh-fernando).
# Local dev: leave unset — browser uses /api via Vite proxy; SSR uses http://localhost:3000.
# Vercel: set to your backend deployment URL (same repo, root project).
VITE_API_URL=https://v0-senesh-fernando.vercel.app

# Public profile links (optional overrides)
VITE_GITHUB_USERNAME=senesh2006
VITE_CONTACT_EMAIL=seneshfernando55@gmail.com
